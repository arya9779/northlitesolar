---
layout: "base.njk"
---
<main class="pt-[72px] md:pt-[80px] bg-background">

    <!-- Hero Section -->
    <section class="max-w-container-max mx-auto px-margin-mobile md:px-margin-desktop mb-16 reveal">
        <div class="relative w-full h-[40vh] md:h-[50vh] rounded-sm overflow-hidden border border-outline-variant/30">
            <img alt="{{ title }} Hero Image" class="absolute inset-0 w-full h-full object-cover" src="{{ hero.image }}"/>
            <div class="absolute inset-0 bg-slate-deep/60 flex items-center justify-center p-6 md:p-8">
                <div class="text-center max-w-3xl">
                    <h1 class="font-display-lg text-headline-lg-mobile md:text-display-lg text-pure-white mb-4 leading-tight font-bold tracking-tight">
                        {{ hero.title }}
                    </h1>
                    <p class="font-body-lg text-body-lg text-secondary-fixed-dim/90">
                        {{ hero.subtitle }}
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Intro Mission Block -->
    <section class="max-w-container-max mx-auto px-margin-mobile md:px-margin-desktop mb-20 reveal">
        <div class="bg-pure-white rounded-sm p-8 md:p-12 border border-outline-variant/30 text-center max-w-4xl mx-auto">
            <span class="material-symbols-outlined text-primary text-5xl mb-4 block" style="font-variation-settings: 'FILL' 1;">volunteer_activism</span>
            <h2 class="font-headline-lg text-headline-lg text-slate-deep mb-6 font-bold">{{ intro.heading }}</h2>
            <p class="font-body-lg text-body-lg text-secondary leading-relaxed">
                {{ intro.body }}
            </p>
        </div>
    </section>

    <!-- Philanthropic Initiatives Section -->
    <section class="max-w-container-max mx-auto px-margin-mobile md:px-margin-desktop mb-24">
        <div class="mb-16 text-center reveal">
            <span class="font-mono text-xs text-primary uppercase tracking-widest mb-3 block">What We Do</span>
            <h2 class="font-headline-lg text-3xl md:text-headline-lg text-slate-deep font-bold">{{ initiatives.title }}</h2>
        </div>

        <div class="tech-grid grid grid-cols-1 lg:grid-cols-3">
            {% for item in initiatives.list %}
            <div class="tech-grid-cell flex flex-col justify-start">
                <!-- Icon badge -->
                <div class="w-12 h-12 rounded-sm {{ item.badge_bg }} flex items-center justify-center mb-6 flex-shrink-0">
                    <span class="material-symbols-outlined text-[26px]" style="font-variation-settings: 'FILL' 1;">{{ item.icon }}</span>
                </div>
                <!-- Title & Description -->
                <h3 class="font-headline-md text-slate-deep mb-4 text-[22px] font-bold">{{ item.title }}</h3>
                <p class="font-body-md text-body-md text-secondary mb-6 leading-relaxed">
                    {{ item.description }}
                </p>
                <!-- Detailed Points -->
                <ul class="space-y-4 font-body-md text-body-md text-on-surface-variant border-t border-outline-variant/20 pt-6 mt-auto">
                    {% for point in item.points %}
                    <li class="flex items-start gap-3">
                        <span class="material-symbols-outlined text-eco-growth text-[20px] mt-0.5 flex-shrink-0">check_circle</span>
                        <span class="leading-normal">{{ point }}</span>
                    </li>
                    {% endfor %}
                </ul>
            </div>
            {% endfor %}
        </div>
    </section>

    <!-- Partnership Section -->
    <section class="max-w-container-max mx-auto px-margin-mobile md:px-margin-desktop mb-24 reveal">
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-gutter items-center bg-pure-white rounded-sm border border-outline-variant/30 p-8 md:p-12">
            <!-- Left Info Panel -->
            <div class="lg:col-span-8">
                <span class="font-mono text-xs text-primary uppercase tracking-widest mb-3 block">Collaboration</span>
                <h3 class="font-headline-lg text-3xl text-slate-deep mb-6 leading-tight font-bold">{{ partnership.title }}</h3>
                <p class="font-body-lg text-body-lg text-secondary leading-relaxed mb-6">
                    {{ partnership.body }}
                </p>
                <p class="font-body-md text-body-md text-secondary italic">
                    {{ partnership.cta_text }}
                </p>
            </div>
            <!-- Right CTA button -->
            <div class="lg:col-span-4 flex justify-center lg:justify-end">
                <a href="mailto:{{ partnership.email }}" class="bg-primary text-pure-white font-mono text-xs uppercase tracking-wider px-8 py-4 rounded-sm hover:bg-slate-deep transition-all flex items-center space-x-2 border border-primary w-full sm:w-auto text-center justify-center">
                    <span>Contact the Foundation</span>
                    <span class="material-symbols-outlined text-[20px]">mail</span>
                </a>
            </div>
        </div>
    </section>

</main>
